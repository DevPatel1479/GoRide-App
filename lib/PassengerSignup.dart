import 'package:flutter/material.dart';

class PassengerSignup extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Passenger Signup Panel... "),
      ),
    );
  }
}
