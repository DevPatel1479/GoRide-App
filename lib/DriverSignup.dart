import 'package:flutter/material.dart';

class DriverSignup extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Driver Signup Panel... "),
      ),
    );
  }
}
